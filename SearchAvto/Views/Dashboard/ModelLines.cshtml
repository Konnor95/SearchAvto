@model IEnumerable<ModelLine>
@{
    ViewBag.Title = "Модельные линии";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}
@section Styles
{
    @Styles.Render("~/bundles/DataTablesStyles")
}
@section Scripts
{
    @Scripts.Render("~/bundles/DataTablesScripts")
    <script>
        $(document).ready(function () {
            $('#main-table').dataTable();
        });
    </script>
}
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header fleft">Модельные линии</h1>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-body">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover" id="main-table" style="max-width: 800px">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Имя</th>
                                <th>Действия</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Id</td>
                                    <td>@Html.ActionLink(item.FullName, "ModelLine", new { id = item.Id })</td>
                                    <td>
                                        @HtmlManager.EditIconLink(Url.Action("EditModelLine", "Dashboard", new { id = item.Id }))
                                        @HtmlManager.DeleteIconLink(Url.Action("DeleteModelLine", "Dashboard", new { id = item.Id }))
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>-
